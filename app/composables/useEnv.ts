// import { H3Event, type EventHandlerRequest } from 'h3';

// export const useEnv = (name: string, event?: H3Event<EventHandlerRequest>) => {
//     const config = useRuntimeConfig(event);
//     return config.public[name];
// }